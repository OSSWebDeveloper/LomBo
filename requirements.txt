# Lombard — shartnoma tizimi uchun kerakli kutubxonalar
# O'rnatish:  pip install -r requirements.txt

# Veb-framework. Django 6 uchun Python 3.12+ kerak.
# PythonAnywhere'da eski Python bo'lsa: Django>=5.2,<6
Django>=5.2,<7.0

# Word (.docx) hujjatlarini o'qish va yasash
python-docx>=1.1.0

# Shablonlarni {{ ... }} belgilari bilan to'ldirish.
# Jinja2 va lxml shu paket bilan birga avtomatik o'rnatiladi.
docxtpl>=0.19.0

# Garov suratlari uchun. Django'ning ImageField'i Pillow'siz ishlamaydi
# (toza kompyuterda `migrate` shu sababli to'xtab qolardi).
Pillow>=10.0

# ---------------------------------------------------------------------------
# PDF haqida: pip paketi kerak emas. PDF Word faylidan aylantiriladi va buning
# uchun tizimda quyidagilardan biri bo'lishi kifoya:
#   Linux/server : LibreOffice  (apt install libreoffice-writer)
#   Windows      : Microsoft Word (PowerShell orqali chaqiriladi)
# Ikkalasi ham bo'lmasa PDF tugmasi ko'rinmaydi, Word yuklab olish ishlaydi.
# ---------------------------------------------------------------------------
